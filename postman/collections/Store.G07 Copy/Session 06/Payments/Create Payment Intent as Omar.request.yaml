$kind: http-request
url: "{{baseUrl}}/api/Payments/order1"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{Messi_Token}}
body:
  type: json
  content: "{}"
auth:
  type: bearer
  credentials:
    token: "{{supabase_service_role_api_key_0ptd}}"
order: 2000
